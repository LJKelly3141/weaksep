% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data-prep.R
\name{as_demand}
\alias{as_demand}
\title{Build a validated demand object from long-format data}
\usage{
as_demand(data, obs, good, price, quantity)
}
\arguments{
\item{data}{A data frame in long format, one row per observation-good pair.}

\item{obs}{Column identifying the observation, such as a time period or a
household. Given either bare or as a character string.}

\item{good}{Column identifying the good. Bare or a character string.}

\item{price}{Column of prices. Must be finite and strictly positive.}

\item{quantity}{Column of quantities. Must be finite and strictly positive.}
}
\value{
An object of class \code{demand}: a list with \code{p} and \code{q}, each a numeric
\verb{T x N} matrix whose rows are observations in sorted order and whose columns
are goods in sorted order, plus \code{goods} (character vector of column names)
and \code{obs_id} (the sorted observation identifiers).
}
\description{
Converts a long data frame of price and quantity observations into the matrix
form used throughout the package, validating it on the way. This is the single
entry point for user data: every test function consumes the object it returns,
so all methods see identical, checked input and are exactly comparable.
}
\examples{
d <- as_demand(sim_cobb_douglas(10, 3, seed = 1), obs, good, price, quantity)
dim(d$p)
colnames(d$p)

}
\seealso{
\code{\link[=weak_separability]{weak_separability()}}, which consumes this object.
}
