% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generate.R
\name{sim_cobb_douglas}
\alias{sim_cobb_douglas}
\title{Simulate demand data from a Cobb-Douglas utility function}
\usage{
sim_cobb_douglas(n_obs, n_goods, blocks = NULL, seed = NULL)
}
\arguments{
\item{n_obs}{Number of observations to generate.}

\item{n_goods}{Number of goods.}

\item{blocks}{Optional named list of character vectors naming goods that form
separable blocks. Names must be among \code{letters[1:n_goods]} when
\code{n_goods <= 26}, otherwise \code{paste0("good_", 1:n_goods)}.}

\item{seed}{Optional integer seed. The caller's random number state is
restored on exit.}
}
\value{
A data frame with columns \code{obs}, \code{good}, \code{price} and \code{quantity}, one
row per observation-good pair, suitable for \code{\link[=as_demand]{as_demand()}}.
}
\description{
Generates price and quantity observations consistent with utility
maximisation. With \code{blocks = NULL} the utility function is a flat
Cobb-Douglas over all goods, which is weakly separable in every partition.
With \code{blocks} supplied, budgeting is two-stage: a fixed share of the budget
goes to each block and fixed shares within it, so the data are blockwise
separable in exactly the given partition. That is the data-generating process
Barnett and Choi (1989) used to show that Varian's procedure rejects weak
separability even when it holds by construction.
}
\examples{
head(sim_cobb_douglas(5, 3, seed = 1))
head(sim_cobb_douglas(5, 4, blocks = list(m = c("a", "b")), seed = 1))

}
\references{
Barnett, W. A., & Choi, S. (1989). A Monte Carlo Study of Tests of Blockwise
Weak Separability. \emph{Journal of Business & Economic Statistics}, 7(3), 363-377.
\doi{10.1080/07350015.1989.10509745}
}
\seealso{
Other generators:
\code{\link[=sim_random]{sim_random()}},
\code{\link[=sim_translog]{sim_translog()}}
}
\concept{generators}
