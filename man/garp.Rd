% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/axioms.R
\name{garp}
\alias{garp}
\title{Test the generalised axiom of revealed preference}
\usage{
garp(p, q, efficiency = 1)
}
\arguments{
\item{p}{Numeric \verb{T x N} matrix of prices; rows are observations, columns are
goods.}

\item{q}{Numeric \verb{T x N} matrix of quantities, same dimensions as \code{p}.}

\item{efficiency}{Numeric scalar in \verb{(0, 1]}. The Afriat efficiency level at
which consistency is assessed; \code{1} is the exact axiom. Lower values weaken
the revealed preference relation and so can only remove violations.}
}
\value{
A list with \code{consistent} (logical), \code{n_violations} (integer),
\code{violations} (a two-column integer matrix of observation index pairs,
possibly with zero rows), and \code{efficiency} (the level tested).
}
\description{
Checks a finite set of price and quantity observations for consistency with
GARP at a given efficiency level, following Varian (1982). The direct revealed
preference relation is built, its transitive closure computed by Warshall's
algorithm, and the closure scanned for pairs contradicted by a strict direct
preference running the other way.
}
\examples{
p <- rbind(c(1, 2), c(2, 1))
q <- rbind(c(1, 2), c(2, 1))
garp(p, q)$consistent
garp(p, q, efficiency = 0.5)$consistent

}
\references{
Varian, H. R. (1982). The Nonparametric Approach to Demand Analysis.
\emph{Econometrica}, 50(4), 945. \doi{10.2307/1912771}
}
\seealso{
Other axioms:
\code{\link[=ccei]{ccei()}},
\code{\link[=sarp]{sarp()}},
\code{\link[=warp]{warp()}}
}
\concept{axioms}
