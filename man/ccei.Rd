% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/axioms.R
\name{ccei}
\alias{ccei}
\title{Critical cost efficiency index}
\usage{
ccei(p, q, tol = 1e-06)
}
\arguments{
\item{p}{Numeric \verb{T x N} matrix of prices; rows are observations, columns are
goods.}

\item{q}{Numeric \verb{T x N} matrix of quantities, same dimensions as \code{p}.}

\item{tol}{Numeric bisection tolerance. The returned value is within \code{tol} of
the true index.}
}
\value{
A numeric scalar in \verb{(0, 1]}.
}
\description{
Computes the Afriat efficiency index of Varian (1990): the largest efficiency
level at which the data satisfy GARP. A value of 1 means the data are exactly
consistent. Lower values measure how far from consistency they are, in the
sense of the fraction of expenditure that must be treated as wasted before the
data can be rationalised.
}
\details{
GARP consistency is monotone in the efficiency level, since lowering it
weakens the direct revealed preference relation and can only remove
violations. The index is therefore found by bisection.
}
\examples{
p <- rbind(c(1, 2), c(2, 1))
q <- rbind(c(1, 2), c(2, 1))
ccei(p, q)

}
\references{
Varian, H. R. (1990). Goodness-of-fit in optimizing models.
\emph{Journal of Econometrics}, 46(1-2), 125-140.
\doi{10.1016/0304-4076(90)90051-T}
}
\seealso{
Other axioms:
\code{\link[=garp]{garp()}},
\code{\link[=sarp]{sarp()}},
\code{\link[=warp]{warp()}}
}
\concept{axioms}
