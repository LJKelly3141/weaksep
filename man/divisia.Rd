% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/index.R
\name{divisia}
\alias{divisia}
\title{Chained Tornqvist-Theil index with Fisher fallback}
\usage{
divisia(p, q, normalization = 1)
}
\arguments{
\item{p}{Numeric \verb{T x N} matrix of prices; rows are observations.}

\item{q}{Numeric \verb{T x N} matrix of quantities, same dimensions as \code{p}.}

\item{normalization}{Numeric scalar, the value of both indices in the first
period.}
}
\value{
A list with \code{price_index} and \code{quantity_index}, each a numeric vector
of length \code{T}, and \code{method_used}, a character vector of length \code{T - 1} with
entries \code{"tornqvist"} or \code{"fisher"}.
}
\description{
Computes chained Tornqvist-Theil price and quantity indices. The
Tornqvist-Theil index is superlative in the sense of Diewert (1976), exact for
a homogeneous translog aggregator, which is what licenses its use as a
subutility proxy in stage two of the Varian separability procedure.
}
\details{
Where the Tornqvist growth factor for a link is not finite or not positive,
typically because an expenditure share is degenerate, the Fisher ideal growth
factor is substituted for that link and a warning names the observations
involved. The \code{method_used} element records which formula produced each link,
so a silent substitution is never possible.
}
\examples{
p <- rbind(c(1, 3), c(2, 6), c(4, 12))
q <- rbind(c(5, 2), c(5, 2), c(5, 2))
divisia(p, q)$price_index

}
\references{
Diewert, W. E. (1976). Exact and superlative index numbers.
\emph{Journal of Econometrics}, 4(2), 115-145.
\doi{10.1016/0304-4076(76)90009-9}
}
\seealso{
Other index numbers:
\code{\link[=fisher]{fisher()}}
}
\concept{index numbers}
