% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/grid.R
\name{separability_grid}
\alias{separability_grid}
\title{Test many candidate partitions at once}
\usage{
separability_grid(
  x,
  partitions,
  method = "fw",
  efficiency = 1,
  subutility = c("afriat", "divisia"),
  solver = NULL,
  adjust = NULL,
  verbose = FALSE
)
}
\arguments{
\item{x}{A \code{demand} object from \code{\link[=as_demand]{as_demand()}}, or a long data frame with
columns \code{obs}, \code{good}, \code{price} and \code{quantity}. It is converted once and
reused, so validation runs a single time no matter how large the grid.}

\item{partitions}{A non-empty list of candidate partitions. Each element is
either a character vector naming the goods in a single candidate group, or a
named list of character vectors for a partition with several groups. Naming
the list labels the rows; unnamed elements are labelled \code{p1}, \code{p2} and so
on, and an unnamed group is called \code{group}.}

\item{method}{Character vector of methods to apply, any of \code{"varian"},
\code{"sw"}, \code{"fw"} and \code{"mip"}. Every method is applied to every partition.}

\item{efficiency}{Numeric vector of Afriat efficiency levels, each in
\verb{(0, 1]}. Every level is applied to every partition and method. Note that
\code{"mip"} and \code{"sw"} accept only \code{1} and will record an error at any other
level rather than silently skipping it.}

\item{subutility}{Stage-two construction for \code{method = "varian"}, either
\code{"afriat"} or \code{"divisia"}. A single value, not a grid dimension; it is not
passed to the other methods, which have their own constructions.}

\item{solver}{Mixed integer solver for \code{"mip"} and \code{"sw"}; see
\code{\link[=mip_solvers]{mip_solvers()}}.}

\item{adjust}{Goods permitted to adjust incompletely under \code{"sw"}.}

\item{verbose}{If \code{TRUE}, emit a message as each cell begins.}
}
\value{
A data frame with one row per cell, in partition-major order, and
columns:
\describe{
\item{\code{partition}}{Label of the candidate partition.}
\item{\code{members}}{Group membership, rendered as \code{"name: good, good"}.}
\item{\code{method}, \code{subutility}, \code{efficiency}}{The cell's settings.
\code{subutility} is \code{NA} for methods that do not use it.}
\item{\code{separable}}{The verdict, or \code{NA} if the cell errored.}
\item{\code{conditions}}{Whether the verdict is \code{"sufficient"},
\code{"necessary"}, or \code{"necessary and sufficient"}.}
\item{\code{ccei}}{The efficiency index reported by the method.}
\item{\code{z}}{Total stage-two adjustment: how far the superlative index had
to move to satisfy Varian's inner inequalities. \code{0} means the raw index
already satisfied them. \code{NA} for methods that produce no such
quantity.}
\item{\code{stage1_pass}, \code{stage2_pass}, \code{stage3_pass}}{Per-stage outcomes.}
\item{\code{n_obs}, \code{n_goods}}{Dimensions of the data.}
\item{\code{error}}{The error message for a cell that failed, else \code{NA}.}
}
The full \code{\link[=weak_separability]{weak_separability()}} objects are attached as the \code{tests}
attribute, one per row and \code{NULL} where a cell errored, for stage detail
the columns do not carry. Subsetting the data frame drops that attribute.
}
\description{
Applies \code{\link[=weak_separability]{weak_separability()}} across a grid of candidate partitions, methods
and efficiency levels, and returns one tidy row per cell. A cell that fails to
run does not stop the grid: its error message is recorded and the remaining
cells are computed, which matters when a method rejects a partition on its own
terms, as \code{"mip"} does for any partition with more than one named group.
}
\section{Comparing methods}{


Passing more than one \code{method} puts the sequential and exact tests side by
side on identical data. Read the \code{conditions} column when doing so. A
\code{separable = FALSE} from \code{"varian"} or \code{"fw"} is inconclusive, because those
conditions are sufficient but not necessary, while a \code{separable = FALSE} from
\code{"mip"} or \code{"sw"} rules separability out. Rows where the sequential test fails
and the exact test passes are the informative ones: they are partitions the
cheap test would have discarded wrongly.
}

\examples{
# Cobb-Douglas utility is additively separable, hence weakly separable in
# every subset of goods. So every candidate below should pass, and a
# rejection here would be a test rejecting something true rather than a
# finding. This is the control case of Barnett and Choi (1989).
d <- sim_cobb_douglas(20, 4, blocks = list(m = c("a", "b")), seed = 1)
separability_grid(d, list(ab = c("a", "b"),
                          ac = c("a", "c"),
                          cd = c("c", "d")))

# Random data satisfies no separability restriction, and fails the first
# stage. Read the `conditions` column before calling that a rejection.
r <- sim_random(20, 4, seed = 3)
separability_grid(r, list(ab = c("a", "b"), cd = c("c", "d")),
                  method = c("varian", "fw"))

# The same partitions at two efficiency levels
separability_grid(d, list(ab = c("a", "b"), ac = c("a", "c")),
                  efficiency = c(1, 0.95))

}
\seealso{
\code{\link[=weak_separability]{weak_separability()}} for a single partition, and
\code{\link[=as.data.frame.weaksep_test]{as.data.frame.weaksep_test()}} for the one-row form of a single result.
}
