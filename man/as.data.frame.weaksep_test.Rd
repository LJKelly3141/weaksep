% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/class-weaksep-test.R
\name{as.data.frame.weaksep_test}
\alias{as.data.frame.weaksep_test}
\title{Coerce a weak separability test result to a one-row data frame}
\usage{
\method{as.data.frame}{weaksep_test}(x, row.names = NULL, optional = FALSE, ...)
}
\arguments{
\item{x}{A \code{weaksep_test} object.}

\item{row.names}{Passed to \code{\link[=data.frame]{data.frame()}}.}

\item{optional}{Ignored, present for S3 consistency.}

\item{...}{Ignored.}
}
\value{
A one-row data frame with columns \code{method}, \code{separable},
\code{conditions}, \code{efficiency}, \code{ccei}, \code{z}, \code{n_obs}, \code{n_goods}, \code{groups}, and
one logical column per stage. \code{z} is the total stage-two adjustment, the
amount the superlative index had to move to satisfy Varian's inner
inequalities, and is \code{NA} for constructions that do not produce one.
}
\description{
Returns a single row so that results from many partitions bind into a tidy
frame with \code{\link[=rbind]{rbind()}}. \code{\link[=separability_grid]{separability_grid()}} does that for you.
}
\examples{
d <- sim_cobb_douglas(15, 4, blocks = list(m = c("a", "b")), seed = 1)
as.data.frame(weak_separability(d, list(m = c("a", "b"))))

}
